# Core dependencies for daily stock analysis
requests==2.31.0
pandas==2.1.4
numpy==1.26.2
yfinance==0.2.33
beautifulsoup4==4.12.2
lxml==5.0.0

# Data visualization
matplotlib==3.8.2
seaborn==0.13.0
plotly==5.18.0

# Technical analysis
ta==0.11.0
pandas-ta==0.3.14b0

# Scheduling and automation
schedule==1.2.1
apscheduler==3.10.4

# Notifications
# Note: smtplib is built into Python stdlib, smtplib2 is a thin wrapper
smtplib2==0.2.1
requests-oauthlib==1.3.1

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Environment and config
python-dotenv==1.0.0
pyyaml==6.0.1

# Logging
loguru==0.7.2

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Development tools
black==23.12.1
flake8==7.0.0
mypy==1.8.0
isort==5.13.2

# Personal additions
ipython==8.18.1  # useful for interactive exploration in the REPL
jupyter==1.0.0  # for notebook-based exploration and charting
rich==13.7.0    # nicer terminal output when running scripts manually
openpyxl==3.1.2  # export analysis results to Excel for easy sharing
tabulate==0.9.0  # pretty-print DataFrames as tables in the terminal
quantStats==0.0.62  # portfolio analytics and tearsheet reports
mplfinance==0.12.10b0  # candlestick charts — much easier than raw matplotlib
# yfinance pinned above — v0.2.34+ changed the Ticker.history() timezone behavior, broke my scripts

# Extra personal tools
nbconvert==7.11.0  # convert notebooks to HTML for easy sharing/archiving
watchdog==3.0.0   # auto-reload scripts when files change during development
scipy==1.11.4     # needed by quantStats for statistical calculations; adding explicitly to avoid version surprises
icedata==0.0.9    # convenient wrapper for fetching A-share data (personal use only, not in upstream)
html5lib==1.1     # fallback HTML parser for beautifulsoup4; lxml can be finicky on some platforms
